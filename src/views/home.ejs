<%- include('partials/head', { title, keywords, desc }) %>
<%- include('partials/header') %>
    <main class="grid">
        <h1><%= toTitleCase(title) %></h1>
        <section class="grid">
            <h2>Welcome to the CSE 340 Service Network</h2>
            <p class="indent">Our mission is to promote service across the world by connecting volunteers with service opportunities in their community.</p>
            <img src="/images/cse340-service-network.png" alt="CSE 340 Service Network Logo">
        </section>
        <% if (isLoggedIn) { %>
            <p><a class="btn" href="/dashboard">My Dashboard</a></p>
        <% } %>
    </main>
<%- include('partials/footer') %>
